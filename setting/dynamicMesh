// AMR
AMR {
	interval = 50;
	maxLevel = 3;
	maxBufferLayer = 2;
	indicatorCellNames = (density);
	indicatorCellValues = (0.0);
	indicatorGradientNames = (density);
	indicatorGradientValues = (100.0);
	maxCells = 20000000;
	minVolume = 1.e-25;
}

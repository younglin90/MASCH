
#include "../../../../others/solvers.h"

void MASCH_Solver::setFValFunctionsUDF(MASCH_Mesh& mesh, MASCH_Control& controls){
	
	// auto& solver = (*this);
	
	// {
		// solver.faceValue.push_back([](double* inpL, double* inpR, double* inpF) ->double 
		// { 
			// double out = inpF[12]*inpL[0] + (1.0-inpL[12])*inpR[0];
			// return out;
		// }); 
	// }
	// {
		// solver.faceValue.push_back([](double* inpL, double* inpR, double* inpF) ->double 
		// { 
			// double out = inpF[0];
			// if(inpF[19]<0.0) out = inpF[1];
			// return out;
		// }); 
	// }
	// {
		// solver.faceValue.push_back([](double* inpL, double* inpR, double* inpF) ->double 
		// { 
			// double out = inpF[0];
			// if(inpF[19]<0.0) out = inpF[1];
			// return out;
		// }); 
	// }
	// {
		// solver.faceValue.push_back([](double* inpL, double* inpR, double* inpF) ->double 
		// { 
			// double out = inpF[0];
			// if(inpF[19]<0.0) out = inpF[1];
			// return out;
		// }); 
	// }
	// {
		// solver.faceValue.push_back([](double* inpL, double* inpR, double* inpF) ->double 
		// { 
			// double out = inpF[0];
			// if(inpF[19]<0.0) out = inpF[1];
			// return out;
		// }); 
	// }
	
}

